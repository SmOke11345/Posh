export enum Pages {
    LOGIN = "login",
    REGISTER = "register",
    PROFILE = "profile",
    CATALOG = "catalog",
    ORDER = "order",
    CHECKOUT = "checkout",
    PRODUCT = "product",
    CART = "cart",
    FAVORITE = "favorite",
}
